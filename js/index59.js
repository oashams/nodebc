var firstNumber = 59;
var secondNumber = 800;
if (firstNumber===secondNumber){
    console.log (`They are the same number`);
} else if (firstNumber>secondNumber) {
    console.log (`${firstNumber} is bigger than the ${secondNumber}`);
} else {
    console.log (`${secondNumber} is bigger than the ${firstNumber}`);
}